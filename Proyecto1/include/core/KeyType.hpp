#pragma once 
/**
 * @brief Enemy identifier, used as the sole targeting key across all
 * 8 registries.
 */
typedef int EnemyId;

/**
 * @brief Alias of EnemyId, used as the key type in ITargetRegistry.
 */
typedef EnemyId Key;
